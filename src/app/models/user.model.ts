export class User{

  constructor(public avatar:string, public name:string,
    public dob:number, public email:string,
    public address:string, public hobbies:string,
    public password:string ){}

}
